#!/bin/sh
# Minimal placeholder for husky helper. When husky is installed, it writes its own helper here.
# This file is included by the generated hooks; keep it harmless if husky isn't installed.
return 0
